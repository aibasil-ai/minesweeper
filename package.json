{
  "name": "minesweeper",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch",
    "dev": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
